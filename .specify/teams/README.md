# Codex Team Runtime

This project exposes the Codex-only team/runtime surface through `sp-teams`.

Optional agent-facing MCP facade: install `specify-cli[mcp]` so `specify-teams-mcp` can be registered in `.codex/config.toml`.
