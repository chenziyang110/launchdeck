# T004 Review Package

## Review Target

- Task brief: `F:/github/launchdeck/.specify/features/2026-07-08-launchdeck-global-cli-control-plane/implementation-review/task-briefs/T004.md`
- Worker result: `F:/github/launchdeck/.specify/teams/state/results/019f413d-0c03-79f2-b907-1f5b6014d3b7.json`
- Changed file: `F:/github/launchdeck/test/control-plane-locks.test.js`

## Review Checks

- Confirm the changed file stays inside allowed write scope.
- Confirm syntax passes and RED state is due to intended missing owner `src/control-plane/locks.js`.
- Confirm tests map to CA-CP-010 and CA-CP-012.
- Confirm test fixture cleanup remains valid once `src/control-plane/locks.js` exists; `createLaunchdeckHome()` cleanup ultimately uses `removeTempProject()` safe-prefix rules from `test/helpers/cli-fixture.js`.
- Confirm tests do not implement lock acquisition or edit production source.

## Validation Evidence

- `node --check test/control-plane-locks.test.js` -> passed
- `node --test test/control-plane-locks.test.js` -> RED, ERR_MODULE_NOT_FOUND for intended owner `src/control-plane/locks.js`

## Controller Notes

- Live T002 learning: custom temp prefixes must still start with `launchdeck-cli-` because `removeTempProject()` refuses other temp project names.

