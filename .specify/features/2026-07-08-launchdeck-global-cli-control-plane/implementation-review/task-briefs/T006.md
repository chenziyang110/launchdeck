# T006 Task Brief

## Objective

Add RED tests in `test/cli-control-plane-contract.test.js` for JSON envelope v1, domain error codes, next actions, and non-interactive refusals.

## Allowed Write Scope

- `F:/github/launchdeck/test/cli-control-plane-contract.test.js`

## Required Evidence

- `node --check test/cli-control-plane-contract.test.js` passes.
- `node --test test/cli-control-plane-contract.test.js` is RED for missing JSON contract behavior, not syntax errors.
- Tests cover schemaVersion, data nesting, default next actions, legacy top-level mirrors, failure code/message/details/next, required error codes, and clean `--all --json` refusal next actions.

## Forbidden Drift

- Do not change `src/output.js` or `src/errors.js` in this test task.
- Do not edit production source.
- Do not remove legacy top-level JSON payload compatibility.

## Worker Result

- Runtime result: `F:/github/launchdeck/.specify/teams/state/results/019f413d-c3a1-7831-babf-b19fb7910ebd.json`
- Reported status: DONE

