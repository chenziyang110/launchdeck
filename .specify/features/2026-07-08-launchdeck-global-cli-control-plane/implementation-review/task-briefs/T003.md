# T003 Task Brief

## Objective

Add RED tests in `test/control-plane-state.test.js` for v2 user-scoped registry state and migration behavior.

## Allowed Write Scope

- `F:/github/launchdeck/test/control-plane-state.test.js`

## Required Evidence

- `node --check test/control-plane-state.test.js` passes.
- `node --test test/control-plane-state.test.js` is RED for missing v2 registry/control-plane behavior, not syntax errors.
- Tests cover v2 `registry/projects.json` creation, legacy `projects.json` read/migration, alias uniqueness, unsupported newer version mutation refusal, and project repair metadata.

## Forbidden Drift

- Do not implement state store in this test task.
- Do not edit production source.
- Do not weaken legacy `LAUNCHDECK_HOME/projects.json` compatibility.

## Worker Result

- Runtime result: `F:/github/launchdeck/.specify/teams/state/results/019f413c-a475-7ce0-8ac3-08ece26bd501.json`
- Reported status: DONE

