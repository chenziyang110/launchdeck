# T002 Task Brief

## Objective

Create `test/helpers/control-plane-fixture.js`, a reusable control-plane test helper module for future state, lock, event, JSON contract, lifecycle, demo, and smoke tests.

## Allowed Write Scope

- `F:/github/launchdeck/test/helpers/control-plane-fixture.js`

## Required Evidence

- The helper exists and passes `node --check`.
- It creates isolated `LAUNCHDECK_HOME` state under a safe temp directory.
- It creates temp Launchdeck project roots using existing fixture safety patterns.
- It provides CLI/JSON runner utilities with isolated env support.
- It writes managed fixture project config and actual script files instead of inline long-running `node -e` commands.
- It supports future demo/test lifecycle validation and CA-CP-020.
- It provides consumer evidence showing future tests can import and use the helper.

## Forbidden Drift

- Do not modify existing `test/helpers/cli-fixture.js` behavior.
- Do not edit production source or task tests in this lane.
- Do not use inline `node -e` long-running fixtures.
- Do not start services outside isolated temp directories.

## Worker Result

- Runtime result: `F:/github/launchdeck/.specify/teams/state/results/019f412d-0b21-7bf3-a71b-ccdbe6eff57e.json`
- Reported status: DONE

