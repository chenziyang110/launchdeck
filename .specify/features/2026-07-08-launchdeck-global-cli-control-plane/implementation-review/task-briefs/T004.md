# T004 Task Brief

## Objective

Add RED tests in `test/control-plane-locks.test.js` for daemonless lock acquisition, metadata, contention, stale lock rules, and release cleanup.

## Allowed Write Scope

- `F:/github/launchdeck/test/control-plane-locks.test.js`

## Required Evidence

- `node --check test/control-plane-locks.test.js` passes.
- `node --test test/control-plane-locks.test.js` is RED for the intended missing owner `src/control-plane/locks.js`.
- Tests cover exclusive creation, owner metadata, bounded wait timeout data, stale-lock refusal/takeover, and cleanup after success/throw.

## Forbidden Drift

- Do not implement lock acquisition in this test task.
- Do not edit production source.
- Do not weaken daemonless file-backed coordination.

## Worker Result

- Runtime result: `F:/github/launchdeck/.specify/teams/state/results/019f413d-0c03-79f2-b907-1f5b6014d3b7.json`
- Reported status: DONE

