# T005 Task Brief

## Objective

Add RED tests in `test/control-plane-events.test.js` for append-only JSONL events, transaction ids, redaction, corrupt-line tolerant reads, and next actions.

## Allowed Write Scope

- `F:/github/launchdeck/test/control-plane-events.test.js`

## Required Evidence

- `node --check test/control-plane-events.test.js` passes.
- `node --test test/control-plane-events.test.js` is RED for the intended missing owner `src/control-plane/events.js`.
- Tests cover append-only JSON Lines, generated IDs/timestamps, caller transaction preservation, secret redaction, corrupt-line tolerant reads, and next action persistence.

## Forbidden Drift

- Do not implement event persistence in this test task.
- Do not edit production source.
- Do not store secret values in expected events.

## Worker Result

- Runtime result: `F:/github/launchdeck/.specify/teams/state/results/019f413d-625f-7521-9a08-c8caef1eba7b.json`
- Reported status: DONE

